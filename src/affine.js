
const affine = {
    encode: function(message, key) {
        // Implementation of Affine Cipher encode
    },

    decode: function(encodedMessage, key) {
        // Implementation of Affine Cipher decode
    }
};

module.exports = affine;
